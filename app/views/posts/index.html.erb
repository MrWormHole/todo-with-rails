<%if false%>
<% @posts.each do |post| %>
  <h1> <%= post.title %></h1>
  <h4> <%= post.content %></h4>
<% end %>
<% end %>

<table>
  <tr>
    <th>ID</th>
    <th>Title</th>
    <th></th>
  </tr>

  <% @posts.each do |post| %>
    <tr>
      <td><%= post.id %></td>
      <td><%= post.title %></td>
      <td><%= link_to 'Show', post_path(post), :class => "btn btn-secondary" %></td>
    </tr>
  <% end %>
</table>

